import { Search } from "lucide-react";
import React from "react";

const SearchBar = () => {
  return (
    <Search className="w-5 h-5 hover:text-shop_btn_light_green hoverEffect" />
  );
};

export default SearchBar;
